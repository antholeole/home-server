import 'package:flutter/material.dart';

/// The Widget that configures yoxdur application.
class MyApp extends StatelessWidget {
  const MyApp({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
      return Container();
  }
}
