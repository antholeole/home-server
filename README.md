HOME MANAGER

Hosts:
- budget
- tasks?
- food!

## getting started

The project is setup so you can open the `code-workspace` in vscode and it is very pretty. 

1. nix
2. direnv
3. run `direnv allow`
4. you also need to install and have a docker daemon running.
5. run `dev`
6. you should forward ports 8080 (hasura engine) and 9695 ports (console).
## TODO 
- hook in treefmt-ni
- add a script that runs ./gradlew build and ./gradlew run 