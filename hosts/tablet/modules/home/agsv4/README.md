## Developing

to code here, you need to first run "ags types" in the this directory.

this will generate an @girs folder with a bunch of types for you.
(troubleshooting: `ags init` somewhere else, then cp the @girs folder over)

Don't deploy agsFull, its huge. Just use it for developing then deploy the
submodules you want
